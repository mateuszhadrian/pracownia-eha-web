---
// Treść detalu realizacji (Etap 4.3) — skin wg eksportu
// docs/design/export/realizacje.html (analiza-realizacje §1–2), markup
// `dt-*` wspólny dla obu progów JEDNEGO overlaya #work-detail
// (WorkDetailOverlay.astro): <1024 bottom sheet (galeria-karuzela
// w strumieniu scrolla), ≥1024 modal (galeria przenoszona przez
// open-detail.ts do lewej kolumny panelu — wzorzec placeGal eksportu).
// Renderowana wewnątrz <template data-work-detail={slug}> i klonowana
// do hosta przy otwarciu; liczniki (REALIZACJA NN/NN, 01/0N) maluje
// open-detail.ts po klonowaniu.
//
// Wideo (E8): preload="none", a miniaturą jest klatka wycięta z samego
// filmu (videoFrameAt, środek liczony z `duration`) — pozycja z filmem
// NIE MA własnego zdjęcia. Klatka JEDNĄ drogą: <img class="dt-poster">
// pod <video>, BEZ atrybutu `poster` (korekta po produkcji 4.3 —
// szczegóły przy mapowaniu galerii niżej; wcześniejsze „dwie drogi"
// delung i tak nie działały w Chromium, które przy preload="none" nie
// pobiera plakatu NIGDY — pomiar szablonu). Odtwarzanie na tap bez
// zmian; `duration` służy też za badge czasu (desktop).
import rycHouse from "@/assets/dom-ryc-house1.webp";
import { imgAt, videoFrameAt } from "@/lib/img";
import { CONTACT_PATH } from "@/lib/routes";
import { WORK_GALLERY_DASHES_MAX } from "./work-config";
import type { ViewProject } from "./work-data";

interface Props {
  project: ViewProject;
}
const { project } = Astro.props as unknown as Props;
const p = project;
const pad = (n: number) => String(n).padStart(2, "0");
// Kreski-wskaźniki tylko przy krótkiej galerii (korekta Mateusza) —
// powyżej progu rząd przestaje się mieścić, zostaje sam licznik.
const showDashes = p.gallery.length <= WORK_GALLERY_DASHES_MAX;
---

<div class="dt-head">
  <span data-projcount>REALIZACJA 01 / 01</span>
  <button class="dt-x" type="button" data-overlay-close aria-label="Zamknij">
    <i aria-hidden="true"><b></b><b></b></i>
  </button>
</div>

<div class="dt-txt">
  {
    /* rycina za prawą krawędzią kolumny treści (eksport: opacity
      .12–.13, multiply) — wersja Z ALFĄ (lekcja 4.2 §2a: blend potrafi
      zgasnąć przy body{position:fixed}, alfa degraduje się niewidocznie) */
  }
  <img
    class="dt-ryc"
    src={rycHouse.src}
    width={rycHouse.width}
    height={rycHouse.height}
    alt=""
    loading="lazy"
    decoding="async"
    aria-hidden="true"
  />
  <div class="dt-meta">
    <span class="dt-place">{p.place}</span>
    <span class="dt-meta-dash" aria-hidden="true"></span>
    <span class="dt-year">{p.year}</span>
  </div>
  <h2 class="dt-title">{p.title}</h2>
</div>

<div class="dt-gal" data-gal>
  <div class="dt-track" data-track>
    {
      p.gallery.map((item, i) => {
        // Klatka-miniatura filmu — WYŁĄCZNIE jako <img class="dt-poster">
        // pod <video>, BEZ atrybutu `poster` (korekta Mateusza po
        // produkcji 4.3): silniki malują obraz z atrybutu ROZCIĄGNIĘTY
        // do pudełka elementu, ignorując object-fit (WebKit), przez co
        // nad poprawnym <img> lądowała zdeformowana klatka — w galerii
        // i jako tło grającego filmu w podglądzie. Atrybut i tak nie był
        // pewną drogą (Chromium przy preload="none" nie pobiera go
        // NIGDY — pomiar szablonu), więc <img> zostaje jedynym źródłem:
        // cover w galerii (wycięty środek wypełnia kadr), contain
        // w podglądzie (czerń wokół — jak sam film).
        const klatka =
          item.type === "video"
            ? videoFrameAt(item.video, item.duration)
            : undefined;
        return (
          <div class="dt-slide" data-slide>
            {item.type === "video" ? (
              <>
                {/* Galeria żyje w <template>, więc obraz nie jest pobierany
                  do czasu otwarcia detalu — zero kosztu dla LCP listy. */}
                {klatka && (
                  <img
                    class="dt-poster"
                    src={klatka}
                    alt=""
                    aria-hidden="true"
                    loading="lazy"
                    decoding="async"
                    draggable="false"
                    style={
                      item.position
                        ? `object-position: ${item.position}`
                        : undefined
                    }
                  />
                )}
                {/* Bez `controls` i bez własnego znaku play (E8):
                  tap/klik w kadr odpala film w podglądzie
                  pełnoekranowym; stan sygnalizuje ikonka kamery. */}
                {/* eslint-disable-next-line astro/jsx-a11y/media-has-caption --
                  klipy realizacji to nieme b-rolle z placu budowy */}
                <video
                  src={item.video}
                  preload="none"
                  playsinline
                  style={
                    item.position
                      ? `object-position: ${item.position}`
                      : undefined
                  }
                />
                {/* Badge „…, aby obejrzeć" w prawym górnym rogu kadru
                  (design eha: tekst na ciemnej plakietce). Dwa warianty
                  treści w SSR zamiast podmiany w JS — przełącza @media
                  w parze z WORK_DESKTOP_MIN_PX. aria-hidden — to
                  dekoracja dla wzroku, nie druga nazwa kadru. Znika
                  i wraca RAZEM z ikonką kamery (reguła .is-playing
                  w WorkDetailOverlay.astro). */}
                <span class="dt-hint" data-cam-hint aria-hidden="true">
                  <span class="dt-hint-d">Kliknij, aby obejrzeć</span>
                  <span class="dt-hint-m">Stuknij, aby obejrzeć</span>
                  {/* Trzeci wariant tej samej plakietki: WSKAŹNIK
                      ŁADOWANIA (sesja poprawek wizualnych). Pokazuje go
                      klasa `.is-loading` na slajdzie podglądu — reguły
                      w WorkDetailOverlay.astro, stan pilnuje
                      open-detail.ts. Powód: hint znikał przy zdarzeniu
                      `play` (65 ms), a obraz ruszał dopiero na `playing`
                      — zmierzone 0,65 s bez throttlingu, 2,0 s na Fast
                      3G i 7,5 s na Slow 3G. Przez ten czas użytkownik
                      widział nieruchomy kadr bez żadnego znaku i brał go
                      za zdjęcie. Kropki są osobnymi elementami, bo
                      animuje je CSS (delayem), nie JS. */}
                  {/* Kropki rysuje `content: "."` z ::before, a nie tekst
                      w DOM: między sąsiednimi <b> prettier wstawia białe
                      znaki, które przeglądarka renderuje jako spacje —
                      wyszłoby „. . ." zamiast „...". Są dekoracją
                      (aria-hidden), więc pseudoelement jest tu na miejscu;
                      testy liczą <b>, a tekstu szukają bez kropek. */}
                  <span class="dt-hint-load">
                    Poczekaj, ładuję wideo
                    <i class="dt-dots" aria-hidden="true">
                      <b />
                      <b />
                      <b />
                    </i>
                  </span>
                </span>
                <span class="dt-cam" data-cam aria-hidden="true">
                  <svg viewBox="0 0 21 14" fill="none">
                    <rect
                      x="0.5"
                      y="2"
                      width="12.5"
                      height="10"
                      rx="2.2"
                      fill="currentColor"
                    />
                    <path d="M14.4 6.2l6-3.4v8.4l-6-3.4z" fill="currentColor" />
                  </svg>
                </span>
                {item.duration && <span class="dt-time">{item.duration}</span>}
              </>
            ) : (
              <img
                src={imgAt(item.image, "full")}
                alt={`${p.title} — zdjęcie ${i + 1}`}
                loading="lazy"
                decoding="async"
                draggable="false"
                style={
                  item.position
                    ? `object-position: ${item.position}`
                    : undefined
                }
              />
            )}
          </div>
        );
      })
    }
  </div>
  <div class="dt-galbar">
    <span class="dt-shotnav">
      <button type="button" data-prevshot aria-label="Poprzednie zdjęcie">
        <svg viewBox="0 0 11 18" fill="none" aria-hidden="true">
          <path
            d="M9.5 1L1.5 9l8 8"
            stroke="currentColor"
            stroke-width="1.5"
            stroke-linecap="round"></path>
        </svg>
      </button>
      <button type="button" data-nextshot aria-label="Następne zdjęcie">
        <svg viewBox="0 0 11 18" fill="none" aria-hidden="true">
          <path
            d="M1.5 1l8 8-8 8"
            stroke="currentColor"
            stroke-width="1.5"
            stroke-linecap="round"></path>
        </svg>
      </button>
    </span>
    <span class="dt-galside">
      {
        showDashes && (
          <span class="dt-dashes" data-dashes>
            {p.gallery.map((_, i) => (
              <button
                type="button"
                data-shot={i}
                class={i === 0 ? "on" : undefined}
                aria-label={`Zdjęcie ${i + 1}`}
              />
            ))}
          </span>
        )
      }
      <span class="dt-count" data-shotcount>01 / {pad(p.gallery.length)}</span>
    </span>
  </div>
</div>

<div class="dt-about">
  {p.paras.map((t) => <p class="dt-desc">{t}</p>)}
</div>

<div class="dt-specs">
  <div class="dt-specs-h">
    <span>PARAMETRY</span>
    <i aria-hidden="true"></i>
  </div>
  <div class="dt-spec-list">
    {
      p.specs.map((s) => (
        <div class="dt-spec">
          <b>{s.label}</b>
          <i aria-hidden="true" />
          <span>{s.value}</span>
        </div>
      ))
    }
  </div>
</div>

<div class="dt-foot">
  <span class="dt-ask-k">CHCESZ WIĘCEJ INFORMACJI?</span>
  <a class="dt-ask" href={CONTACT_PATH}>
    Skontaktuj się z nami
    <svg viewBox="0 0 16 10" aria-hidden="true">
      <path
        d="M0 5h14M10.4 1.2L14.2 5l-3.8 3.8"
        fill="none"
        stroke="currentColor"
        stroke-width="1.5"
        stroke-linecap="round"></path>
    </svg>
  </a>
</div>

<style>
  /* ── Mobile (<1024): jeden strumień scrolla w dt-body (sheet) ── */
  .dt-head {
    display: none;
  }

  /* dt-body (sheet) to flex column ze scrollem — bloki treści muszą być
     nieściśliwe (flex: none), inaczej overflow:hidden zeruje ich
     automatyczny minimalny rozmiar i flex zgniata je do paddingu */
  .dt-txt,
  .dt-gal,
  .dt-about,
  .dt-specs,
  .dt-foot {
    flex: none;
  }
  .dt-txt {
    position: relative;
    overflow: hidden;
    padding: 4px 24px 0;
  }
  .dt-ryc {
    position: absolute;
    right: -40px;
    top: -14px;
    width: 150px;
    max-width: none;
    height: auto;
    opacity: 0.13;
    mix-blend-mode: multiply;
    pointer-events: none;
  }
  .dt-meta {
    position: relative;
    display: flex;
    align-items: baseline;
    gap: 10px;
    margin-bottom: 9px;
    font-family: var(--font-mono), monospace;
    font-weight: 500;
    font-size: 10px;
    letter-spacing: 0.18em;
    color: rgba(87, 101, 74, 0.95);
  }
  .dt-place {
    text-transform: uppercase;
  }
  .dt-meta-dash {
    flex: none;
    width: 14px;
    height: 1px;
    background: rgba(87, 101, 74, 0.5);
  }
  .dt-title {
    position: relative;
    margin: 0;
    font-family: var(--font-serif), serif;
    font-weight: 500;
    font-size: clamp(26px, 7.4vw, 29px);
    line-height: 1.14;
    color: var(--ink);
    text-wrap: pretty;
    padding-right: 34px;
  }

  /* ── Galeria: karuzela snap 4:3 (eksport: 300 px + gap 10) ── */
  .dt-gal {
    margin-top: 18px;
    position: relative;
  }
  .dt-track {
    display: flex;
    /* gap zaszyty też w JS: open-detail liczy `offsetWidth + 10` —
       zmiana gapu wymaga zmiany PARY (twarde założenie portu) */
    gap: 10px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-padding-left: 24px;
    padding-inline: 24px;
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
  }
  .dt-track::-webkit-scrollbar {
    display: none;
  }
  .dt-slide {
    flex: 0 0 300px;
    aspect-ratio: 4 / 3;
    position: relative;
    overflow: hidden;
    background: var(--bg-dark);
    scroll-snap-align: start;
    /* kafel po kaflu — gotcha karuzel (sections.md) */
    scroll-snap-stop: always;
    /* tap/klik w kadr otwiera podgląd pełnoekranowy */
    cursor: zoom-in;
  }
  .dt-slide img,
  .dt-slide video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: sepia(0.16) saturate(0.88);
  }
  /* ikonka kamery (lewy górny róg kadru wideo) — znika, gdy film gra
     (klasa is-playing na slajdzie podglądu; w galerii film nie gra) */
  .dt-cam {
    position: absolute;
    top: 12px;
    left: 12px;
    width: 40px;
    height: 40px;
    border-radius: 999px;
    background: rgba(12, 10, 8, 0.55);
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
  }
  .dt-cam svg {
    width: 21px;
    height: 14px;
  }
  /* badge „Stuknij/Kliknij, aby obejrzeć" — prawy górny róg kadru,
     ciemna plakietka (design eha). Nie łapie zdarzeń — tap w kadr
     ma otwierać podgląd, tak jak przy ikonce kamery. */
  .dt-hint {
    position: absolute;
    top: 14px;
    right: 10px;
    max-width: calc(100% - 68px);
    padding: 5px 9px;
    background: rgba(12, 10, 8, 0.55);
    font-family: var(--font-mono), monospace;
    font-weight: 500;
    font-size: 9px;
    line-height: 1.3;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    text-align: right;
    color: #f5efe3;
    pointer-events: none;
  }
  .dt-hint-d {
    display: none;
  }
  /* Wariant „ładuję" siedzi w SSR obok dwóch pozostałych i domyślnie
     jest schowany — odsłania go klasa `.is-loading` na slajdzie podglądu
     (reguły + animacja kropek: blok is:global w WorkDetailOverlay.astro,
     bo klasę nadaje open-detail.ts na sklonowanym węźle). */
  .dt-hint-load {
    display: none;
  }
  /* Kropki „…" jako trzy elementy — animuje je CSS delayem, nie JS.
     Bez ruchu (reduce / brak animacji) muszą być WIDOCZNE, więc stan
     spoczynku to opacity 1, a animacja dopiero go zbija. */
  .dt-dots {
    font-style: normal;
  }
  .dt-dots b {
    font-weight: inherit;
  }
  .dt-dots b::before {
    content: ".";
  }
  .dt-time {
    display: none;
  }

  /* pasek pod karuzelą: kreski-wskaźniki po lewej + licznik po prawej */
  .dt-galbar {
    display: flex;
    align-items: center;
    padding: 11px 24px 0;
  }
  .dt-shotnav {
    display: none;
  }
  .dt-galside {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
  }
  .dt-dashes {
    display: flex;
    align-items: center;
    gap: 5px;
  }
  .dt-dashes button {
    appearance: none;
    border: none;
    padding: 0;
    cursor: pointer;
    width: 8px;
    height: 2px;
    background: rgba(33, 29, 24, 0.2);
    transition:
      width 0.2s ease,
      background 0.2s ease;
  }
  .dt-dashes button.on {
    width: 16px;
    background: var(--accent);
  }
  .dt-count {
    font-family: var(--font-mono), monospace;
    font-weight: 500;
    font-size: 10px;
    line-height: 1;
    letter-spacing: 0.12em;
    color: var(--faint);
    /* licznik trzyma prawą krawędź także BEZ kresek (galeria > próg —
       wskaźniki nie są wtedy renderowane) */
    margin-left: auto;
  }

  /* ── Akapity ── */
  .dt-about {
    padding: 22px 24px 0;
  }
  .dt-desc {
    margin: 0;
    font-family: var(--font-body), sans-serif;
    font-size: clamp(14px, 3.85vw, 14.5px);
    line-height: 1.75;
    color: var(--faint);
    text-wrap: pretty;
  }
  .dt-desc + .dt-desc {
    margin-top: 13px;
  }

  /* ── PARAMETRY: wiersze z kropkowanym wypełniaczem (design) ── */
  .dt-specs {
    margin-top: 26px;
    padding: 0 24px 34px;
  }
  .dt-specs-h {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 12px;
    font-family: var(--font-mono), monospace;
    font-weight: 500;
    font-size: 10px;
    line-height: 1;
    letter-spacing: 0.2em;
    color: rgba(87, 101, 74, 0.95);
  }
  .dt-specs-h i {
    flex: 1;
    height: 1px;
    background: rgba(33, 29, 24, 0.14);
  }
  .dt-spec-list {
    display: flex;
    flex-direction: column;
    gap: 9px;
  }
  .dt-spec {
    display: flex;
    align-items: baseline;
    gap: 8px;
  }
  .dt-spec b {
    font-family: var(--font-body), sans-serif;
    font-weight: 400;
    font-size: 13px;
    color: rgba(33, 29, 24, 0.62);
  }
  .dt-spec i {
    flex: 1;
    height: 0;
    border-bottom: 1px dotted rgba(33, 29, 24, 0.3);
  }
  .dt-spec span {
    font-family: var(--font-mono), monospace;
    font-weight: 500;
    font-size: 12.5px;
    text-align: right;
    color: var(--ink);
  }

  /* ── Stopka CTA: mobile przyklejona do dołu strumienia sheeta.
     Papier zamiast płaskiego #F1ECE0 z eksportu i BEZ border-top
     (korekta Mateusza) — te same warstwy co dt-body, statyczne
     (element sticky nie scrolluje); separację robi sam cień. ── */
  .dt-foot {
    position: sticky;
    bottom: 0;
    margin-top: auto;
    background:
      linear-gradient(rgba(250, 247, 241, 0.8), rgba(250, 247, 241, 0.8)),
      url("../../../assets/paper-background.webp") bottom center / 100% auto
        repeat-y #faf7f1;
    box-shadow: 0 -6px 18px rgba(24, 19, 14, 0.06);
    padding: 12px 24px 20px;
    display: flex;
    flex-direction: column;
    gap: 7px;
  }
  .dt-ask-k {
    font-family: var(--font-mono), monospace;
    font-weight: 500;
    font-size: 9.5px;
    line-height: 1;
    letter-spacing: 0.18em;
    color: rgba(33, 29, 24, 0.62);
  }
  .dt-ask {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 9px;
    text-decoration: none;
    background: #26221b;
    color: #f5efe3;
    font-family: var(--font-body), sans-serif;
    font-weight: 500;
    font-size: 13.5px;
    padding: 14px 18px;
    border-radius: 2px;
    transition: background 0.2s ease;
  }
  .dt-ask:hover {
    background: #332d24;
  }
  .dt-ask svg {
    flex: none;
    width: 15px;
    height: 10px;
  }

  /* ════ Desktop (≥1024, para: WORK_DESKTOP_MIN_PX): modal — galeria
     w lewej kolumnie 57 % (JS move), treść scrolluje w prawej ════ */
  @media (min-width: 1024px) {
    .dt-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 20px;
      padding: clamp(24px, 2.1vw, 32px) clamp(34px, 3.33vw, 53px) 0;
      flex: none;
      font-family: var(--font-mono), monospace;
      font-weight: 500;
      font-size: 10px;
      line-height: 1;
      letter-spacing: 0.18em;
      color: var(--faint);
    }
    .dt-x {
      appearance: none;
      border: 1px solid rgba(33, 29, 24, 0.12);
      background: rgba(236, 232, 221, 0.9);
      padding: 0;
      cursor: pointer;
      width: 44px;
      height: 44px;
      margin: -6px -14px 0 0;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .dt-x:hover {
      background: #e3decf;
    }
    .dt-x i {
      position: relative;
      width: 14px;
      height: 14px;
      display: block;
    }
    .dt-x i b {
      position: absolute;
      top: 6px;
      left: 0;
      width: 14px;
      height: 1.6px;
      background: var(--ink);
    }
    .dt-x i b:first-child {
      transform: rotate(45deg);
    }
    .dt-x i b:last-child {
      transform: rotate(-45deg);
    }

    .dt-txt {
      overflow: visible;
      padding: clamp(16px, 1.5vw, 24px) clamp(34px, 3.33vw, 53px) 0;
    }
    .dt-ryc {
      right: 0;
      top: 14px;
      width: clamp(140px, 12.5vw, 200px);
      opacity: 0.12;
    }
    .dt-meta {
      font-size: 11px;
      margin-bottom: clamp(11px, 0.97vw, 16px);
    }
    .dt-meta-dash {
      width: 16px;
    }
    .dt-title {
      font-size: clamp(26px, 2.5vw, 40px);
      line-height: 1.1;
      padding-right: 40px;
    }

    /* galeria po przeniesieniu do .dt (lewa kolumna 57 % — design) */
    .dt-gal {
      order: 0;
      flex: none;
      width: 57%;
      height: 100%;
      margin: 0;
      background: var(--bg-dark);
      overflow: hidden;
    }
    .dt-gal::after {
      content: "";
      position: absolute;
      left: 0;
      right: 0;
      bottom: 0;
      height: 180px;
      pointer-events: none;
      background: linear-gradient(
        180deg,
        rgba(24, 19, 14, 0),
        rgba(24, 19, 14, 0.6)
      );
    }
    .dt-track {
      height: 100%;
      gap: 0;
      /* visible, NIE hidden z eksportu: overflow:hidden na torze
         przycinałby przesunięte translateX slajdy we współrzędnych
         lokalnych (klipuje .dt-gal — fix wady makiety) */
      overflow: visible;
      padding: 0;
      scroll-snap-type: none;
      transition: transform 0.62s cubic-bezier(0.16, 1, 0.3, 1);
    }
    .dt-slide {
      width: 100%;
      height: 100%;
      flex-basis: 100%;
      aspect-ratio: auto;
      scroll-snap-align: none;
    }
    /* kamera desktop 4× większa niż mobilne 40 px (korekty Mateusza:
       najpierw 8×, po obejrzeniu zmniejszona o połowę — 160 px;
       mobile zostaje przy 40 px) */
    .dt-cam {
      top: 24px;
      left: 24px;
      width: 160px;
      height: 160px;
    }
    .dt-cam svg {
      width: 52.5%;
      height: auto;
    }
    .dt-hint {
      top: 16px;
      right: 14px;
      font-size: 10px;
      max-width: calc(100% - 76px);
    }
    .dt-hint-d {
      display: inline;
    }
    .dt-hint-m {
      display: none;
    }
    .dt-time {
      display: block;
      position: absolute;
      right: 24px;
      bottom: 94px;
      z-index: 2;
      padding: 6px 11px;
      background: rgba(12, 10, 8, 0.5);
      font-family: var(--font-mono), monospace;
      font-weight: 500;
      font-size: 11px;
      letter-spacing: 0.12em;
      color: #fff;
    }

    /* pasek galerii na kadrze: licznik lewy dolny róg, strzałki prawy
       (design) — row-reverse odwraca kolejność markupu */
    .dt-galbar {
      position: absolute;
      left: 0;
      right: 0;
      bottom: 0;
      z-index: 2;
      display: flex;
      flex-direction: row-reverse;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      padding: 22px 24px 22px 26px;
    }
    .dt-shotnav {
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .dt-shotnav button {
      appearance: none;
      padding: 0;
      cursor: pointer;
      width: 52px;
      height: 52px;
      box-sizing: border-box;
      border: 1px solid rgba(245, 239, 227, 0.5);
      background: none;
      color: #f5efe3;
      display: flex;
      align-items: center;
      justify-content: center;
      transition:
        opacity 0.35s ease,
        background 0.3s ease;
    }
    .dt-shotnav button:hover {
      background: rgba(245, 239, 227, 0.16);
    }
    .dt-shotnav button[disabled] {
      opacity: 0.35;
      cursor: default;
    }
    .dt-shotnav svg {
      width: 10px;
      height: 17px;
    }
    .dt-galside {
      flex: none;
    }
    /* kreski-wskaźniki żyją na mobile — desktop ma licznik + strzałki */
    .dt-dashes {
      display: none;
    }
    .dt-count {
      font-size: 11px;
      letter-spacing: 0.16em;
      color: rgba(245, 239, 227, 0.9);
    }

    .dt-about {
      padding: clamp(20px, 1.81vw, 29px) clamp(34px, 3.33vw, 53px) 0;
    }
    .dt-desc {
      font-size: clamp(13.5px, 1.08vw, 17px);
      line-height: 1.78;
    }
    .dt-desc + .dt-desc {
      margin-top: clamp(11px, 0.97vw, 16px);
    }

    .dt-specs {
      margin-top: clamp(20px, 1.81vw, 29px);
      padding: 0 clamp(34px, 3.33vw, 53px) 0;
    }
    .dt-spec-list {
      gap: 9px;
    }
    .dt-spec b {
      font-size: clamp(11.5px, 0.94vw, 14.8px);
    }
    .dt-spec span {
      font-size: clamp(10.5px, 0.86vw, 13.6px);
    }

    /* CTA jako blok w strumieniu treści (design: za linią, na końcu) */
    .dt-foot {
      position: static;
      margin-top: clamp(6px, 0.6vw, 10px);
      padding: clamp(20px, 1.81vw, 29px) clamp(34px, 3.33vw, 53px)
        clamp(32px, 3.06vw, 49px);
      background: none;
      border-top: none;
      box-shadow: none;
      align-items: flex-start;
      gap: 12px;
    }
    .dt-foot::before {
      content: "";
      display: block;
      width: 100%;
      height: 1px;
      background: rgba(33, 29, 24, 0.14);
      margin-bottom: clamp(8px, 0.8vw, 13px);
    }
    .dt-ask-k {
      font-size: 9.5px;
      letter-spacing: 0.18em;
    }
    .dt-ask {
      display: inline-flex;
      gap: 10px;
      font-size: clamp(13.5px, 1.08vw, 17px);
      padding: clamp(13px, 1.11vw, 18px) clamp(22px, 1.94vw, 31px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .dt-track,
    .dt-ask,
    .dt-shotnav button,
    .dt-dashes button {
      transition: none;
    }
  }
</style>
